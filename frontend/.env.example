# ─────────────────────────────────────────────────────────────
#  Zomato Clone — Frontend Environment Variables
#  Copy this file to .env and fill in your own values.
#  Never commit .env to version control.
#
#  All variables MUST be prefixed with VITE_ to be exposed
#  to the browser by Vite.
# ─────────────────────────────────────────────────────────────

# ── Backend API URL ──────────────────────────────────────────
# Point this to your running backend server.
# Default fallback in axios.js: http://localhost:4005/api/v1
VITE_API_URL=http://localhost:4005/api/v1
